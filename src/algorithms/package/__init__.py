# __init__.py


# executes even before any sub module import

# packages are also considered modules
# that can pack multiple submodules. Kind of namespaces,
# to modularize/organize code and avoid name collisions ( global variable etc )
print("Importing the package package")
